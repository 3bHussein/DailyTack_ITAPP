@model IEnumerable<WebApplication1.Models.DailyLogTb>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section style{
    <link href="~/Content/jquery-ui.css" rel="stylesheet" />
    <link href="~/Content/datepicker.css" rel="stylesheet" />
    <style>


        #Satement {
            max-width: 1000px;
            width: 100% !important;
            height: 150px !important;
        }
    </style>

}


<!DOCTYPE html>

<html dir="ltr">
<head>
    <meta name="viewport" content="width=device-width" />
    <title>fullsearch</title>
</head>


<body>
    <br />


    @*@Html.ActionLink("Print", "Export", "DailyLogTbs", new { area = "" }, new { @class = "btn btn-danger" })*@
    @*<input value="Print Win" onclick="window.print();" type="button" />
        <input value="Print BOdy" style="width:512px;" onclick="printDiv('pro')" type="button" />*@


    <button onclick="window.print();" dir="rtl">
        @*<img src="~/Content/print.png" style="width:100px;height:100px;" />*@
        <img src="~/Content/icons8-print-100.png" style="width:50px" />
    </button>

    <div class="" dir="rtl">

        @using (Html.BeginForm("fullsearch1", "DailyLogTbs", FormMethod.Post))
        {

            <div class="row form-horizontal">

                @Html.TextBox("search1", null, new { @class = "form-control col-md-11", placeholder = "search", style = "max-width:500px" })


                <input type="submit" style="direction:ltr" value="searchByData" class="btn btn-outline-secondary col-md-2" />|
                @Html.ActionLink("Back To list", "index", new { area = "" }, new { @class = "btn btn-info" })

            </div>
        }


        @*@using (Html.BeginForm("fullsearch", "DailyLogTbs", FormMethod.Post))
            {

                <div class="row form-horizontal">

                    @Html.TextBox("search", null, new { @class = "form-control col-md-11", placeholder = "search", style = "max-width:500px" })

                    <input type="submit" value="searchByName" class="btn btn-primary col-md-1" />|
                    @Html.ActionLink("Back To list", "index", new { area = "" }, new { @class = "btn btn-info" })

                </div>
            }*@

    </div>
    @*<p>
            @Html.ActionLink("Create New", "Create")
        </p>*@
    <table class="table" id="pro">
        <tr>
            <th style="text-align:right">
                @*@Html.DisplayNameFor(model => model.Day)*@
                اليوم
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.DateOfDay)*@
                التاريخ
            </th>
            <th style="text-align:right">
                @*@Html.DisplayNameFor(model => model.Satement)*@
                البيان
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td style="text-align:right">
                    @Html.DisplayFor(modelItem => item.Day)
                </td>
                <td style="text-align:left">
                    @Html.DisplayFor(modelItem => item.DateOfDay)
                </td>
                <td style="text-align:right">
                    @Html.DisplayFor(modelItem => item.Satement)
                </td>
                @*<td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-success" }) |
                        @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-warning" }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-danger" })



                    </td>*@
            </tr>
        }
        <Hr class="btn-warning" />
    </table>




    @section Scripts {
        @*<script>
                function PrintElem(elem) {
                    var mywindow = window.open('', 'PRINT', 'height=400,width=600');

                    mywindow.document.write('<html><head><title>' + document.title + '</title>');
                    mywindow.document.write('</head><body >');
                    mywindow.document.write('<table>' + document. + '</table>');
                    mywindow.document.write(document.getElementById(elem).innerHTML);
                    mywindow.document.write('</body></html>');

                    mywindow.document.close(); // necessary for IE >= 10
                    mywindow.focus(); // necessary for IE >= 10*/

                    mywindow.print();
                    mywindow.close();

                    return true;
                }
            </script>*@

        @*<script>
            //function printcon(el) {
            //    var re = document.body.innerHTML;
            //    var prcon = document.getElementById(el).innerHTML;
            //    document.body.innerHTML = prcon;
            //    window.print();
            //}

            function printDiv(divName) {
                var printContents = document.getElementById(divName).innerHTML;
                var originalContents = document.body.innerHTML;
                document.body.innerHTML = printContents;
                window.print();
                document.body.innerHTML = originalContents;
            }
        </script>*@
            <script type="text/javascript">

                $(function () {
                    $("#search1").datepicker
                        ({
                            dateFormat: 'yy-mm-dd',
                            showStatus: true,
                            showWeeks: true,
                            highlightWeek: true,
                            numberOfMonths: 1,
                            showAnim: "clip",
                            //showAnim: "fold",
                            showOptions: {
                                origin: ["top", "left"]
                            }
                        });
                });
            </script>
   


    }
</body>
</html>
